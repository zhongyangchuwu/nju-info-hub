{
  "name": "@nju-info/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "export-feeds": "tsx src/export-feeds-cli.ts",
    "test": "vitest run src static/catalog",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "@nju-info/db": "workspace:*"
  }
}
